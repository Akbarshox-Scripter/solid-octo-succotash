-- 🧊 СИЙ v9.0 | ULTRA GLASSMORPHISM KEY SYSTEM + IY CORE
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")

local Player = Players.LocalPlayer
local Mouse = Player:GetMouse()
local Camera = workspace.CurrentCamera

-- Защита UI
local success, guiParent = pcall(function() return gethui() end)
if not success or not guiParent then pcall(function() guiParent = CoreGui end) end
if not guiParent then guiParent = Player:WaitForChild("PlayerGui") end

pcall(function() if guiParent:FindFirstChild("SIY_KeySystem") then guiParent.SIY_KeySystem:Destroy() end end)
pcall(function() if guiParent:FindFirstChild("SIY_IYHub") then guiParent.SIY_IYHub:Destroy() end end)
pcall(function() if guiParent:FindFirstChild("SIY_PunishGui") then guiParent.SIY_PunishGui:Destroy() end end)

-- Создаем GUI для ключа
local KeyGui = Instance.new("ScreenGui")
KeyGui.Name = "SIY_KeySystem"
KeyGui.Parent = guiParent
KeyGui.ResetOnSpawn = false
KeyGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Главный фрейм ключа (Имбовый Glassmorphism стиль)
local KeyFrame = Instance.new("Frame")
KeyFrame.Name = "KeyFrame"
KeyFrame.Size = UDim2.new(0, 0, 0, 0) -- Начинаем с нуля для анимации
KeyFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
KeyFrame.AnchorPoint = Vector2.new(0.5, 0.5)
KeyFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
KeyFrame.BackgroundTransparency = 0.35
KeyFrame.BorderSizePixel = 0
KeyFrame.ClipsDescendants = true
KeyFrame.Parent = KeyGui

local UICorner = Instance.new("UICorner", KeyFrame)
UICorner.CornerRadius = UDim.new(0, 16)

local UIStroke = Instance.new("UIStroke", KeyFrame)
UIStroke.Color = Color3.fromRGB(138, 43, 226)
UIStroke.Transparency = 0.2
UIStroke.Thickness = 2.5

local UIGradient = Instance.new("UIGradient", KeyFrame)
UIGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(180, 180, 220))
})
UIGradient.Transparency = NumberSequence.new({
    NumberSequenceKeypoint.new(0, 0.2),
    NumberSequenceKeypoint.new(1, 0.5)
})
UIGradient.Rotation = 45

-- Текстовая метка для печатания
local TitleLabel = Instance.new("TextLabel", KeyFrame)
TitleLabel.Size = UDim2.new(1, -40, 0, 40)
TitleLabel.Position = UDim2.new(0, 20, 0, 25)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = ""
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 22
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Center

-- Поле ввода ключа
local KeyInput = Instance.new("TextBox", KeyFrame)
KeyInput.Size = UDim2.new(1, -40, 0, 45)
KeyInput.Position = UDim2.new(0, 20, 0, 85)
KeyInput.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
KeyInput.BackgroundTransparency = 0.4
KeyInput.PlaceholderText = "Введи ключ сюда..."
KeyInput.Text = ""
KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyInput.PlaceholderColor3 = Color3.fromRGB(130, 130, 150)
KeyInput.TextSize = 14
KeyInput.Font = Enum.Font.GothamMedium
Instance.new("UICorner", KeyInput).CornerRadius = UDim.new(0, 10)
local InputStroke = Instance.new("UIStroke", KeyInput)
InputStroke.Color = Color3.fromRGB(80, 80, 110)
InputStroke.Thickness = 1.5

-- Кнопка Activate
local ActivateBtn = Instance.new("TextButton", KeyFrame)
ActivateBtn.Size = UDim2.new(1, -40, 0, 45)
ActivateBtn.Position = UDim2.new(0, 20, 0, 145)
ActivateBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
ActivateBtn.Text = "Activate"
ActivateBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ActivateBtn.TextSize = 16
ActivateBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", ActivateBtn).CornerRadius = UDim.new(0, 10)

-- Анимация появления: маленький -> нормальный
TweenService:Create(KeyFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 360, 0, 215)
}):Play()

-- Эффект печатания текста "Введите ключ" по очереди
task.spawn(function()
    task.wait(0.4)
    local fullText = "Введите ключ"
    for i = 1, #fullText do
        TitleLabel.Text = string.sub(fullText, 1, i)
        task.wait(0.06)
    end
end)

-- Функция запуска основного скрипта (IY Hub)
local function launchMainHub()
    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "SIY_IYHub"
    ScreenGui.Parent = guiParent
    ScreenGui.ResetOnSpawn = false

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = UDim2.new(0, 480, 0, 300)
    MainFrame.Position = UDim2.new(0.5, -240, 0.5, -150)
    MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
    MainFrame.BackgroundTransparency = 0.1
    MainFrame.BorderSizePixel = 0
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local UICorner = Instance.new("UICorner", MainFrame)
    UICorner.CornerRadius = UDim.new(0, 10)
    local UIStroke = Instance.new("UIStroke", MainFrame)
    UIStroke.Color = Color3.fromRGB(138, 43, 226)
    UIStroke.Thickness = 2

    local TopBar = Instance.new("Frame", MainFrame)
    TopBar.Size = UDim2.new(1, 0, 0, 35)
    TopBar.BackgroundTransparency = 1

    local Title = Instance.new("TextLabel", TopBar)
    Title.Size = UDim2.new(0, 300, 1, 0)
    Title.Position = UDim2.new(0, 10, 0, 0)
    Title.BackgroundTransparency = 1
    Title.Text = "⚡ СИЙ v9.0 | PURE IY CORE"
    Title.TextColor3 = Color3.fromRGB(255, 255, 255)
    Title.TextSize = 14
    Title.Font = Enum.Font.GothamBold
    Title.TextXAlignment = Enum.TextXAlignment.Left

    local function createWinBtn(text, color, posX)
        local btn = Instance.new("TextButton", TopBar)
        btn.Size = UDim2.new(0, 26, 0, 26)
        btn.Position = UDim2.new(1, posX, 0, 4)
        btn.BackgroundColor3 = color
        btn.BackgroundTransparency = 0.3
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.TextSize = 16
        btn.Font = Enum.Font.GothamBold
        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
        return btn
    end

    local CloseBtn = createWinBtn("×", Color3.fromRGB(255, 60, 60), -30)
    local MinimizeBtn = createWinBtn("-", Color3.fromRGB(255, 170, 0), -60)

    CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

    local minimized = false
    MinimizeBtn.MouseButton1Click:Connect(function()
        minimized = not minimized
        TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            Size = minimized and UDim2.new(0, 480, 0, 35) or UDim2.new(0, 480, 0, 300)
        }):Play()
    end)

    local dragging, dragStart, startPos
    TopBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true dragStart = input.Position startPos = MainFrame.Position
            input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)

    local Sidebar = Instance.new("Frame", MainFrame)
    Sidebar.Size = UDim2.new(0, 120, 1, -40)
    Sidebar.Position = UDim2.new(0, 0, 0, 35)
    Sidebar.BackgroundTransparency = 1

    local TabIndicator = Instance.new("Frame", Sidebar)
    TabIndicator.Size = UDim2.new(1, -14, 0, 28)
    TabIndicator.Position = UDim2.new(0, 7, 0, 6)
    TabIndicator.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    TabIndicator.BackgroundTransparency = 0.3
    Instance.new("UICorner", TabIndicator).CornerRadius = UDim.new(0, 6)

    local Pages = Instance.new("Folder", MainFrame)
    local function createPage()
        local p = Instance.new("ScrollingFrame", Pages)
        p.Size = UDim2.new(1, -130, 1, -40)
        p.Position = UDim2.new(0, 125, 0, 35)
        p.BackgroundTransparency = 1
        p.Visible = false
        p.ScrollBarThickness = 3
        return p
    end

    local PageMain = createPage()
    local PageCmds = createPage()
    local PageSettings = createPage()
    PageMain.Visible = true

    local Commands = {}
    local FLYING = false
    local IY_SPEED = 50
    local flyKeyDown, flyKeyUp

    Commands.fly = function()
        local char = Player.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        if FLYING then return end
        FLYING = true
        local hrp = char.HumanoidRootPart
        local bg = Instance.new("BodyGyro", hrp)
        bg.P = 9e4 bg.maxTorque = Vector3.new(9e4, 9e4, 9e4) bg.cframe = hrp.CFrame
        local bv = Instance.new("BodyVelocity", hrp)
        bv.velocity = Vector3.new(0,0,0) bv.maxForce = Vector3.new(9e4, 9e4, 9e4)
        local ctrl = {f = 0, b = 0, l = 0, r = 0}
        flyKeyDown = Mouse.KeyDown:Connect(function(key)
            if key:lower() == "w" then ctrl.f = 1 elseif key:lower() == "s" then ctrl.b = -1 elseif key:lower() == "a" then ctrl.l = -1 elseif key:lower() == "d" then ctrl.r = 1 end
        end)
        flyKeyUp = Mouse.KeyUp:Connect(function(key)
            if key:lower() == "w" then ctrl.f = 0 elseif key:lower() == "s" then ctrl.b = 0 elseif key:lower() == "a" then ctrl.l = 0 elseif key:lower() == "d" then ctrl.r = 0 end
        end)
        char:FindFirstChildOfClass("Humanoid").PlatformStand = true
        task.spawn(function()
            while FLYING and char do
                RunService.RenderStepped:Wait()
                if hrp then
                    bg.cframe = Camera.CFrame
                    local moveDir = char:FindFirstChildOfClass("Humanoid").MoveDirection
                    if moveDir.Magnitude > 0 then
                        bv.velocity = moveDir * IY_SPEED
                    else
                        bv.velocity = (Camera.CFrame.LookVector * (ctrl.f + ctrl.b) + Camera.CFrame.RightVector * (ctrl.r + ctrl.l)) * IY_SPEED
                    end
                end
            end
        end)
    end

    Commands.unfly = function()
        FLYING = false
        if flyKeyDown then flyKeyDown:Disconnect() end
        if flyKeyUp then flyKeyUp:Disconnect() end
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            for _,v in pairs(char.HumanoidRootPart:GetChildren()) do
                if v:IsA("BodyGyro") or v:IsA("BodyVelocity") then v:Destroy() end
            end
        end
        if char and char:FindFirstChildOfClass("Humanoid") then
            char:FindFirstChildOfClass("Humanoid").PlatformStand = false
        end
    end

    local noclipLoop
    Commands.noclip = function()
        if noclipLoop then noclipLoop:Disconnect() end
        noclipLoop = RunService.Stepped:Connect(function()
            if Player.Character then
                for _, v in pairs(Player.Character:GetDescendants()) do
                    if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end
                end
            end
        end)
    end

    Commands.clip = function() if noclipLoop then noclipLoop:Disconnect() end end

    Commands.fling = function()
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local bav = Instance.new("BodyAngularVelocity", char.HumanoidRootPart)
            bav.Name = "IY_Fling" bav.AngularVelocity = Vector3.new(0, 99999, 0) bav.MaxTorque = Vector3.new(0, math.huge, 0) bav.P = math.huge
        end
    end

    Commands.unfling = function()
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") and char.HumanoidRootPart:FindFirstChild("IY_Fling") then
            char.HumanoidRootPart.IY_Fling:Destroy()
        end
    end

    local walkflingLoop
    Commands.walkfling = function()
        local char = Player.Character
        if not char then return end
        walkflingLoop = RunService.Stepped:Connect(function()
            if char and char:FindFirstChild("HumanoidRootPart") then
                char.HumanoidRootPart.Velocity = char.HumanoidRootPart.Velocity + Vector3.new(0, 5000, 0)
                task.wait(0.01)
                char.HumanoidRootPart.Velocity = char.HumanoidRootPart.Velocity + Vector3.new(0, -5000, 0)
            end
        end)
    end

    Commands.unwalkfling = function() if walkflingLoop then walkflingLoop:Disconnect() end end

    Commands.speed = function(args)
        local char = Player.Character
        if char and char:FindFirstChildOfClass("Humanoid") then
            local amt = args[1] and tonumber(args[1]) or 100
            char:FindFirstChildOfClass("Humanoid").WalkSpeed = amt IY_SPEED = amt
        end
    end

    Commands.jump = function(args)
        local char = Player.Character
        if char and char:FindFirstChildOfClass("Humanoid") then
            local amt = args[1] and tonumber(args[1]) or 100
            char:FindFirstChildOfClass("Humanoid").UseJumpPower = true
            char:FindFirstChildOfClass("Humanoid").JumpPower = amt
        end
    end

    Commands.btools = function()
        for i = 1, 4 do Instance.new("HopperBin", Player.Backpack).BinType = i end
    end

    Commands.invis = function()
        local char = Player.Character
        if char then
            for _,v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") or v:IsA("Decal") then v.Transparency = 1 end
            end
        end
    end

    Commands.vis = function()
        local char = Player.Character
        if char then
            for _,v in pairs(char:GetDescendants()) do
                if v:IsA("BasePart") and v.Name ~= "HumanoidRootPart" then v.Transparency = 0 end
                if v:IsA("Decal") then v.Transparency = 0 end
            end
        end
    end

    Commands.esp = function()
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= Player and p.Character then
                local hl = Instance.new("Highlight", p.Character)
                hl.Name = "IY_ESP" hl.FillColor = Color3.new(1,0,0) hl.OutlineColor = Color3.new(1,1,1)
            end
        end
    end

    Commands.unesp = function()
        for _, p in pairs(Players:GetPlayers()) do
            if p.Character and p.Character:FindFirstChild("IY_ESP") then p.Character.IY_ESP:Destroy() end
        end
    end

    Commands.sit = function()
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            Player.Character:FindFirstChildOfClass("Humanoid").Sit = true
        end
    end

    Commands.god = function()
        if Player.Character and Player.Character:FindFirstChildOfClass("Humanoid") then
            Player.Character:FindFirstChildOfClass("Humanoid").MaxHealth = math.huge
            Player.Character:FindFirstChildOfClass("Humanoid").Health = math.huge
        end
    end

    local spinLoop
    Commands.spin = function()
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            local bav = Instance.new("BodyAngularVelocity", char.HumanoidRootPart)
            bav.Name = "IY_Spin" bav.AngularVelocity = Vector3.new(0, 50, 0) bav.MaxTorque = Vector3.new(0, math.huge, 0)
        end
    end

    Commands.unspin = function()
        local char = Player.Character
        if char and char:FindFirstChild("HumanoidRootPart") and char.HumanoidRootPart:FindFirstChild("IY_Spin") then
            char.HumanoidRootPart.IY_Spin:Destroy()
        end
    end

    Commands.goto = function(args)
        if not args[1] then return end
        local target
        for _, p in pairs(Players:GetPlayers()) do
            if string.lower(p.Name):sub(1, #args[1]) == string.lower(args[1]) then target = p break end
        end
        if target and target.Character and Player.Character then
            Player.Character:PivotTo(target.Character:GetPivot())
        end
    end

    local function executeCommand(rawInput)
        if not rawInput or rawInput == "" then return end
        local split = string.split(rawInput, " ")
        local cmdName = string.lower(table.remove(split, 1))
        if Commands[cmdName] then pcall(function() Commands[cmdName](split) end) end
    end

    local CmdInput = Instance.new("TextBox", PageMain)
    CmdInput.Size = UDim2.new(1, -55, 0, 32)
    CmdInput.Position = UDim2.new(0, 8, 0, 8)
    CmdInput.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
    CmdInput.PlaceholderText = "Команда (напр: goto Name)"
    CmdInput.Text = ""
    CmdInput.TextColor3 = Color3.fromRGB(255, 255, 255)
    CmdInput.TextSize = 11
    CmdInput.Font = Enum.Font.Gotham
    Instance.new("UICorner", CmdInput).CornerRadius = UDim.new(0, 6)
    Instance.new("UIStroke", CmdInput).Color = Color3.fromRGB(60, 60, 80)

    local ExecBtn = Instance.new("TextButton", PageMain)
    ExecBtn.Size = UDim2.new(0, 38, 0, 32)
    ExecBtn.Position = UDim2.new(1, -42, 0, 8)
    ExecBtn.BackgroundColor3 = Color3.fromRGB(138, 43, 226)
    ExecBtn.Text = "▶"
    ExecBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
    Instance.new("UICorner", ExecBtn).CornerRadius = UDim.new(0, 6)

    ExecBtn.MouseButton1Click:Connect(function() executeCommand(CmdInput.Text) CmdInput.Text = "" end)
    CmdInput.FocusLost:Connect(function(enter) if enter then executeCommand(CmdInput.Text) CmdInput.Text = "" end end)

    local BtnContainer = Instance.new("Frame", PageMain)
    BtnContainer.Size = UDim2.new(1, -16, 1, -50)
    BtnContainer.Position = UDim2.new(0, 8, 0, 48)
    BtnContainer.BackgroundTransparency = 1

    local GridLayout = Instance.new("UIGridLayout", BtnContainer)
    GridLayout.CellSize = UDim2.new(0.48, 0, 0, 30)
    GridLayout.CellPadding = UDim2.new(0, 8, 0, 8)

    local function addBtn(name, cmd)
        local b = Instance.new("TextButton", BtnContainer)
        b.BackgroundColor3 = Color3.fromRGB(30, 30, 45)
        b.Text = name
        b.TextColor3 = Color3.fromRGB(220, 220, 240)
        b.TextSize = 11
        b.Font = Enum.Font.GothamBold
        Instance.new("UICorner", b).CornerRadius = UDim.new(0, 6)
        b.MouseButton1Click:Connect(function() executeCommand(cmd) end)
    end

    addBtn("Fly", "fly") addBtn("Unfly", "unfly") addBtn("Noclip", "noclip") addBtn("Clip", "clip")
    addBtn("Fling", "fling") addBtn("Unfling", "unfling") addBtn("Walkfling", "walkfling") addBtn("Unwalkfling", "unwalkfling")
    addBtn("Invis", "invis") addBtn("Vis", "vis") addBtn("ESP", "esp") addBtn("Unesp", "unesp")
    addBtn("Btools", "btools") addBtn("Sit", "sit") addBtn("God", "god") addBtn("Spin", "spin")
    addBtn("Unspin", "unspin") addBtn("Speed 100", "speed 100") addBtn("Jump 100", "jump 100") addBtn("Speed 16", "speed 16")

    PageMain.CanvasSize = UDim2.new(0, 0, 0, 450)

    local CmdText = Instance.new("TextLabel", PageCmds)
    CmdText.Size = UDim2.new(1, -16, 1, -16)
    CmdText.Position = UDim2.new(0, 8, 0, 8)
    CmdText.BackgroundTransparency = 1
    CmdText.Text = "Доступные команды в строке:\n\ngoto [Имя]\nspeed [число]\njump [число]\n\nА также все кнопки из Главной вкладки работают как текстовые команды."
    CmdText.TextColor3 = Color3.fromRGB(200, 200, 220)
    CmdText.TextSize = 12
    CmdText.Font = Enum.Font.GothamMedium
    CmdText.TextXAlignment = Enum.TextXAlignment.Left
    CmdText.TextYAlignment = Enum.TextYAlignment.Top

    local rainbowActive = false
    local rainbowConnection

    local function updateThemeColor(color)
        UIStroke.Color = color
        TabIndicator.BackgroundColor3 = color
        ExecBtn.BackgroundColor3 = color
   
